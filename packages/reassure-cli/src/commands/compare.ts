export const command = 'compare';
export const describe = 'compares gathered baseline and current measurements';

export function handler(argv: any) {
  console.log('Running compare command', argv);
}
