# Git & PR Workflow

## Commit messages

Conventional-style subjects: `chore: update deps`, `feat: use async render`, `docs: update link`. Keep subjects short and imperative.

## Changesets

Include a Changeset for any user-facing package change (`yarn changeset`).

## Pull requests

- Reference the related issue
- Note any docs impact
- Confirm `yarn validate` passes
